<div class="rounded-3xl bg-slate-900/70 border border-white/5 shadow-2xl shadow-slate-900/50 p-6">
  <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4">
    <div class="space-y-1">
      <div class="flex items-center gap-2 text-xs text-slate-400">
        <%= link_to salas_path, class: "inline-flex items-center justify-center h-7 w-7 rounded-full border border-white/10 text-white hover:border-sky-400 hover:text-sky-300" do %>
          <i class="fa-arrow-left"></i>
        <% end %>
        <span>Sala</span>
      </div>
      <h2 class="text-2xl font-bold text-white flex items-center gap-3">
        <span><%= sala.nome %></span>
        <% if sala.admin?(current_usuario) %>
          <span class="rounded-full bg-emerald-500/20 text-emerald-200 text-[12px] px-3 py-1 font-semibold border border-emerald-500/40">Administrador</span>
        <% else %>
          <span class="rounded-full bg-slate-700/80 text-slate-200 text-[12px] px-3 py-1 font-semibold border border-white/5">Membro</span>
        <% end %>
      </h2>
      <p class="text-sm text-slate-300 max-w-2xl">Organize conversas, defina papéis e categorias para manter o time alinhado.</p>
    </div>
    <div class="flex flex-wrap gap-2">
      <% if sala.admin?(current_usuario) %>
        <%= link_to 'Editar sala', edit_sala_path(sala), class: 'inline-flex items-center gap-2 rounded-xl border border-white/10 px-4 py-2 text-white hover:border-sky-400' %>
        <%= button_to 'Excluir', sala_path(sala), method: :delete, data: { turbo_confirm: 'Tem certeza que deseja excluir a sala?' }, class: 'inline-flex items-center gap-2 rounded-xl bg-red-600/90 text-white px-4 py-2 hover:bg-red-500' %>
      <% end %>
    </div>
  </div>
</div>
