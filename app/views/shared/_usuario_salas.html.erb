<section class="flex flex-col w-full">
  <span class="text-white text-center text-sm mt-5">Usuário: <strong><%= current_usuario.email %></strong></span>
  <div class="flex items-center justify-center mt-2">
    <%= button_to 'Sair', destroy_usuario_session_path, method: :delete, data: { turbo: false }, class: 'px-3 py-1 text-white bg-red-700 rounded font-semibold text-sm' %>
  </div>
  <h3 class="text-xl font-bold text-white mt-3 text-center mb-2">Minhas Salas</h3>

  <div class="flex flex-col px-5 gap-2 w-full" id="usuario_<%= current_usuario.id %>_salas">
    <% current_usuario.salas.all.each do |sala| %>
      <%= render 'shared/sala', sala: sala, usuario_atual: current_usuario %>
    <% end %>
  </div>
</section>
