<%= turbo_frame_tag "mensagem_#{@mensagem.id}", class: 'rounded-2xl border border-white/5 bg-slate-900/60 px-4 py-3 shadow-lg shadow-slate-900/40' do %>
  <%= render 'mensagens/form', { sala: @sala, mensagem: @mensagem } %>
<% end %>
