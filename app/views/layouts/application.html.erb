<!DOCTYPE html>
<html>
  <head>
    <title>ChatApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-omc22gnb6jioFp9WlH0I1GtIsfRSAxEPPGjBJOCa/MEGLjm+rXhN3kLBXjg5eQof8RP4e+OeXtfLOcAfeU3u6Q==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen">
    <div class="min-h-screen flex flex-col">
      <header class="border-b border-white/10 bg-slate-900/60 backdrop-blur-md">
        <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
          <%= link_to root_path, data: { turbo_frame: '_top' }, class: "flex items-center gap-2" do %>
            <div class="h-10 w-10 rounded-2xl bg-gradient-to-br from-sky-500 to-emerald-400 flex items-center justify-center font-bold text-slate-900 shadow-lg shadow-sky-500/30">
              C
            </div>
            <div>
              <p class="text-lg font-semibold text-white">ChatApp</p>
              <p class="text-xs text-slate-300">Rails 7 · Hotwire · Tailwind</p>
            </div>
          <% end %>
          <% if usuario_signed_in? %>
            <div class="flex items-center gap-3 text-sm text-slate-200">
              <span class="hidden sm:inline">Olá, <strong><%= current_usuario.email %></strong></span>
              <%= button_to 'Sair', destroy_usuario_session_path, method: :delete, data: { turbo: false }, class: 'px-3 py-1 rounded-lg bg-slate-800 border border-white/10 hover:border-sky-400 text-white text-xs font-semibold' %>
            </div>
          <% end %>
        </div>
      </header>

      <main class="flex-1">
        <%= yield %>
      </main>
    </div>
  </body>
</html>
